# ملف المستخدم وميزات تسجيل الخروج التلقائي
# User Profile & Auto-Logout Features

## نظرة عامة (Overview)

تم إضافة صفحة ملف شخصي متكاملة مع ميزة تسجيل الخروج التلقائي بعد فترة سكون قابلة للتخصيص.

---

## المزايا الجديدة (New Features)

### 1. صفحة الملف الشخصي (Profile Page) 👤

#### الوصول للصفحة:
- من قائمة المستخدم في الأعلى (اضغط على الصورة الرمزية)
- اختر "الملف الشخصي"

#### الأقسام:

##### أ) معلومات المستخدم (Sidebar)
- صورة رمزية بالحرف الأول من الاسم
- الاسم الكامل
- الدور (admin, accountant, sales, etc.)
- البريد الإلكتروني
- الحالة (نشط/غير نشط)
- آخر تسجيل دخول
- تاريخ إنشاء الحساب

##### ب) المعلومات الشخصية (Personal Information)
- تحديث الاسم الكامل
- تحديث رقم الهاتف
- زر حفظ التغييرات

##### ج) تغيير كلمة المرور (Change Password)
- إدخال كلمة المرور الحالية
- إدخال كلمة المرور الجديدة (6 أحرف على الأقل)
- تأكيد كلمة المرور الجديدة
- زر إظهار/إخفاء كلمة المرور لكل حقل 👁️
- التحقق من تطابق كلمات المرور

##### د) إدارة الجلسة (Session Management) ⏰
- **تفعيل/تعطيل تسجيل الخروج التلقائي**
  - مفتاح تبديل (Toggle Switch)
  - عند التفعيل، تظهر خيارات إضافية

- **اختيار مدة السكون:**
  - 5 دقائق
  - 10 دقائق
  - 15 دقيقة (افتراضي)
  - 30 دقيقة
  - ساعة واحدة
  - ساعتين

- **شرح النشاط:**
  - تحريك الماوس
  - الضغط على لوحة المفاتيح
  - النقر على أي عنصر في الصفحة

---

## كيفية عمل تسجيل الخروج التلقائي

### التتبع الذكي للنشاط:
يتم مراقبة الأنشطة التالية:
- `mousedown` - الضغط على الماوس
- `mousemove` - تحريك الماوس
- `keypress` - الضغط على أي مفتاح
- `scroll` - التمرير في الصفحة
- `touchstart` - اللمس على الشاشات التي تعمل باللمس
- `click` - النقر على أي عنصر

### آلية العمل:
1. عند تسجيل الدخول، يتم تحميل الإعدادات من localStorage
2. إذا كان التسجيل التلقائي مفعّلاً، يبدأ العداد
3. عند أي نشاط من المستخدم، يتم إعادة تعيين العداد
4. إذا انتهت المدة المحددة بدون نشاط:
   - يظهر إشعار: "تم تسجيل الخروج تلقائياً بسبب عدم النشاط"
   - يتم تسجيل الخروج تلقائياً
   - يعود المستخدم إلى صفحة تسجيل الدخول

### حفظ الإعدادات:
- يتم حفظ الإعدادات في `localStorage`
- تبقى الإعدادات محفوظة حتى بعد إغلاق المتصفح
- يتم تطبيقها تلقائياً في كل مرة يسجل فيها المستخدم دخوله

---

## الاستخدام (How to Use)

### تحديث المعلومات الشخصية:
1. افتح صفحة "الملف الشخصي"
2. عدّل الاسم أو رقم الهاتف
3. اضغط "حفظ التغييرات"
4. ستظهر رسالة نجاح

### تغيير كلمة المرور:
1. أدخل كلمة المرور الحالية
2. أدخل كلمة المرور الجديدة (6 أحرف على الأقل)
3. أدخل تأكيد كلمة المرور
4. اضغط "تحديث كلمة المرور"
5. ستظهر رسالة نجاح

### تفعيل تسجيل الخروج التلقائي:
1. افتح صفحة "الملف الشخصي"
2. انتقل إلى قسم "إدارة الجلسة"
3. فعّل مفتاح "تسجيل خروج تلقائي عند السكون"
4. اختر المدة المناسبة من القائمة المنسدلة
5. اضغط "حفظ إعدادات الجلسة"
6. ستظهر رسالة نجاح

---

## التفاصيل التقنية (Technical Details)

### الملفات الجديدة:

#### `modules/profile.html`
صفحة HTML كاملة تحتوي على:
- واجهة المستخدم للملف الشخصي
- نماذج المعلومات الشخصية
- نموذج تغيير كلمة المرور
- إعدادات الجلسة
- تصميم responsive مع Tailwind CSS

#### `js/profile.js`
ملف JavaScript يحتوي على:
- `initProfileModule()` - تهيئة الصفحة
- `loadUserProfile()` - تحميل بيانات المستخدم
- `wirePersonalInfoForm()` - ربط نموذج المعلومات الشخصية
- `wirePasswordForm()` - ربط نموذج تغيير كلمة المرور
- `wireSessionSettings()` - ربط إعدادات الجلسة
- `loadSessionSettings()` - تحميل إعدادات الجلسة المحفوظة
- `wirePasswordToggles()` - ربط أزرار إظهار/إخفاء كلمة المرور

### الملفات المعدّلة:

#### `js/auth.js`
إضافات جديدة:
- متغيرات التحكم:
  - `_idleTimer` - مؤقت السكون
  - `_idleTimeoutMs` - مدة السكون بالميلي ثانية
  - `_autoLogoutEnabled` - حالة التفعيل

- وظائف جديدة:
  - `_resetIdleTimer()` - إعادة تعيين مؤقت السكون
  - `_performAutoLogout()` - تنفيذ تسجيل الخروج التلقائي
  - `_setupIdleDetection()` - إعداد رصد النشاط
  - `_configureAutoLogout()` - تكوين تسجيل الخروج التلقائي
  - `_loadAutoLogoutSettings()` - تحميل الإعدادات من localStorage

- إضافة إلى `window.AppAuth`:
  - `configureAutoLogout(enabled, timeoutMinutes)` - API عامة
  - `getCurrentUser()` - الحصول على المستخدم الحالي

- مستمعات الأحداث:
  - `auth:signedIn` - تحميل إعدادات تسجيل الخروج
  - `auth:signedOut` - إيقاف المؤقت

#### `js/main.js`
- إضافة وحدة `profile` إلى `MODULES`:
```javascript
profile: { label: 'الملف الشخصي', actions: ['view'] }
```

#### `index.html`
- إضافة زر "الملف الشخصي" في قائمة المستخدم:
```html
<button data-module="profile" class="module-btn ...">
    <i class="fas fa-user-circle"></i>
    <span>الملف الشخصي</span>
</button>
```

---

## الأمان (Security)

### تغيير كلمة المرور:
- يستخدم Supabase Auth API مباشرة
- `auth.updateUser({ password: newPassword })`
- التحقق من طول كلمة المرور (6 أحرف على الأقل)
- التحقق من تطابق كلمة المرور وتأكيدها

### تسجيل الخروج التلقائي:
- يحمي من جلسات مفتوحة على أجهزة مشتركة
- قابل للتخصيص حسب احتياجات الأمان
- يمكن تعطيله للمستخدمين الموثوق بهم

### تخزين الإعدادات:
- يتم تخزين الإعدادات في `localStorage` فقط
- لا يتم إرسال الإعدادات إلى السيرفر
- آمن للاستخدام على الأجهزة الشخصية

---

## الأسئلة الشائعة (FAQ)

### س: هل يمكن تعطيل تسجيل الخروج التلقائي؟
**ج:** نعم، يمكنك تعطيله من صفحة الملف الشخصي، قسم "إدارة الجلسة".

### س: ماذا يحدث إذا كنت أعمل وانتهت المدة؟
**ج:** سيتم تسجيل خروجك. تأكد من حفظ عملك بانتظام، أو اختر مدة أطول.

### س: هل النشاط في تبويب آخر يُحسب؟
**ج:** لا، النشاط يُحسب فقط في تبويب النظام المفتوح.

### س: كيف أعرف أن التسجيل التلقائي مفعّل؟
**ج:** سترى المفتاح مفعّلاً في صفحة الملف الشخصي، قسم "إدارة الجلسة".

### س: هل يمكن للمدير تفعيل التسجيل التلقائي لجميع المستخدمين؟
**ج:** حالياً، كل مستخدم يتحكم في إعداداته الخاصة.

---

## الاختبار (Testing)

### اختبار صفحة الملف الشخصي:
- [ ] فتح الصفحة من قائمة المستخدم
- [ ] عرض المعلومات الشخصية بشكل صحيح
- [ ] تحديث الاسم الكامل
- [ ] تحديث رقم الهاتف
- [ ] تغيير كلمة المرور بنجاح
- [ ] التحقق من تطابق كلمات المرور

### اختبار تسجيل الخروج التلقائي:
- [ ] تفعيل الميزة
- [ ] اختيار مدة قصيرة (5 دقائق)
- [ ] الانتظار بدون نشاط
- [ ] التحقق من ظهور الإشعار
- [ ] التحقق من تسجيل الخروج التلقائي
- [ ] تعطيل الميزة والتحقق من عدم التسجيل التلقائي

### اختبار زر إظهار/إخفاء كلمة المرور:
- [ ] الضغط على الزر في حقل كلمة المرور الحالية
- [ ] الضغط على الزر في حقل كلمة المرور الجديدة
- [ ] الضغط على الزر في حقل تأكيد كلمة المرور
- [ ] التحقق من تغيير الأيقونة (عين/عين مغلقة)

---

## التحسينات المستقبلية (Future Enhancements)

1. **إشعار قبل تسجيل الخروج:**
   - عداد تنازلي (60 ثانية)
   - زر "أنا هنا" لإلغاء التسجيل التلقائي

2. **سجل النشاط:**
   - عرض آخر 10 عمليات تسجيل دخول
   - عرض الأجهزة المستخدمة
   - IP addresses

3. **إعدادات متقدمة:**
   - تفعيل المصادقة الثنائية (2FA)
   - إدارة الأجهزة الموثوقة
   - تسجيل خروج من جميع الأجهزة

4. **تحسينات الأمان:**
   - فرض تغيير كلمة المرور كل X يوم
   - سياسات كلمة المرور القوية
   - تنبيهات أمنية

---

## الملاحظات (Notes)

- جميع التغييرات تُحفظ فوراً في قاعدة البيانات
- الإعدادات محفوظة في localStorage للمتصفح الحالي فقط
- تسجيل الخروج التلقائي يعمل فقط عند تفعيله
- يمكن للمستخدم تغيير إعداداته في أي وقت

---

**تاريخ الإصدار:** 2026-05-11
**الإصدار:** 1.0.0
**المطور:** Claude Sonnet 4.5
