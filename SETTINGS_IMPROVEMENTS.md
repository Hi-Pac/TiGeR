# تحسينات إعدادات النظام - Settings Module Improvements

## نظرة عامة (Overview)

تم إضافة قسم جديد شامل إلى إعدادات النظام يسمى **البيانات المرجعية** لإدارة جميع القوائم المنسدلة والبيانات الأساسية في النظام.

## المزايا الجديدة (New Features)

### 1. إدارة تصنيفات الأصناف (Product Categories Management)
- إضافة تصنيفات جديدة للأصناف
- تعديل التصنيفات الموجودة
- حذف التصنيفات
- عرض جميع التصنيفات في جدول منظم

### 2. إدارة وحدات القياس (Product Units Management)
- إضافة وحدات قياس جديدة (قطعة، كرتونة، علبة، إلخ)
- تعديل الوحدات الموجودة
- حذف الوحدات
- إضافة اختصارات للوحدات

### 3. إدارة المخازن (Warehouses Management)
- إضافة مخازن جديدة
- تعديل بيانات المخازن
- حذف المخازن
- إضافة كود المخزن والموقع
- تحديث تلقائي للقوائم المنسدلة في جميع أنحاء النظام بعد الإضافة أو التعديل

### 4. إدارة الفروع (Branches Management)
- إضافة فروع جديدة
- تعديل بيانات الفروع
- حذف الفروع
- إضافة كود الفرع والعنوان

## الملفات المعدلة (Modified Files)

### 1. `modules/settings.html`
- إضافة تبويب جديد "البيانات المرجعية"
- إضافة 4 أقسام فرعية:
  - تصنيفات الأصناف
  - وحدات القياس
  - المخازن
  - الفروع
- كل قسم يحتوي على:
  - نموذج إدخال مع حقول مخفية للتعديل
  - جدول لعرض البيانات
  - أزرار للتعديل والحذف

### 2. `js/settings.js`
إضافة الوظائف التالية:

#### Field References (مراجع الحقول)
```javascript
- productCategoryForm, productCategoryIdField, productCategoryNameField, productCategoryDescField
- productUnitForm, productUnitIdField, productUnitNameField, productUnitAbbrField
- warehouseForm, warehouseIdField, warehouseNameField, warehouseCodeField, warehouseLocationField
- branchForm, branchIdField, branchNameField, branchCodeField, branchAddressField
```

#### Load Functions (وظائف التحميل)
```javascript
- loadProductCategories() - تحميل التصنيفات من قاعدة البيانات
- loadProductUnits() - تحميل وحدات القياس
- loadWarehousesForRefData() - تحميل المخازن
- loadBranches() - تحميل الفروع
```

#### Render Functions (وظائف العرض)
```javascript
- renderProductCategoriesTable() - عرض جدول التصنيفات
- renderProductUnitsTable() - عرض جدول الوحدات
- renderWarehousesTable() - عرض جدول المخازن
- renderBranchesTable() - عرض جدول الفروع
```

#### Event Handlers (معالجات الأحداث)
- معالجات submit للنماذج الأربعة (create/update)
- معالجات cancel للإلغاء ومسح النماذج
- معالجات edit لتحميل البيانات في النموذج
- معالجات delete لحذف السجلات

## كيفية الاستخدام (How to Use)

### إضافة تصنيف جديد (Add New Category)
1. انتقل إلى إعدادات النظام > البيانات المرجعية
2. في قسم "تصنيفات الأصناف"
3. أدخل اسم التصنيف والوصف
4. اضغط "حفظ"
5. سيظهر التصنيف الجديد في الجدول

### تعديل تصنيف موجود (Edit Category)
1. في جدول التصنيفات، اضغط على أيقونة التعديل (✏️)
2. ستظهر بيانات التصنيف في النموذج
3. عدل البيانات المطلوبة
4. اضغط "حفظ"

### حذف تصنيف (Delete Category)
1. في جدول التصنيفات، اضغط على أيقونة الحذف (🗑️)
2. أكد عملية الحذف
3. سيتم حذف التصنيف من قاعدة البيانات

**ملاحظة:** نفس الخطوات تنطبق على وحدات القياس والمخازن والفروع.

## التحسينات التقنية (Technical Improvements)

### 1. Integration with Database
- الربط المباشر مع جداول قاعدة البيانات
- استخدام `window.DB` API للعمليات
- دعم RLS (Row Level Security)

### 2. Real-time Updates
- تحديث تلقائي للجداول بعد الإضافة/التعديل/الحذف
- تحديث القوائم المنسدلة في الإعدادات العامة والمخزون والمبيعات

### 3. User Experience
- رسائل نجاح/خطأ واضحة
- تأكيد قبل الحذف
- تنظيف النماذج تلقائياً بعد الحفظ
- عرض حالة كل سجل (نشط/غير نشط)

### 4. Form Validation
- التحقق من الحقول المطلوبة
- عرض رسائل تحذير عند ترك الحقول فارغة
- منع إرسال النماذج الفارغة

## Database Tables (جداول قاعدة البيانات)

الجداول المستخدمة:
```
- product_categories (id, company_id, name, name_ar, description, status)
- product_units (id, company_id, name, name_ar, abbreviation, status)
- warehouses (id, company_id, name, code, location, status)
- branches (id, company_id, name, code, address, status)
```

## Security (الأمان)

- جميع العمليات تحترم RLS policies
- عزل البيانات حسب company_id
- استخدام prepared statements للحماية من SQL injection
- التحقق من الصلاحيات قبل العمليات

## Testing Checklist (قائمة الاختبار)

### تصنيفات الأصناف
- [ ] إضافة تصنيف جديد
- [ ] تعديل تصنيف موجود
- [ ] حذف تصنيف
- [ ] عرض التصنيفات في الجدول
- [ ] التحقق من الحقول المطلوبة

### وحدات القياس
- [ ] إضافة وحدة جديدة
- [ ] إضافة اختصار للوحدة
- [ ] تعديل وحدة موجودة
- [ ] حذف وحدة
- [ ] عرض الوحدات في الجدول

### المخازن
- [ ] إضافة مخزن جديد
- [ ] إضافة كود وموقع للمخزن
- [ ] تعديل مخزن موجود
- [ ] حذف مخزن
- [ ] التحقق من تحديث القوائم المنسدلة تلقائياً

### الفروع
- [ ] إضافة فرع جديد
- [ ] إضافة كود وعنوان للفرع
- [ ] تعديل فرع موجود
- [ ] حذف فرع
- [ ] عرض الفروع في الجدول

### الوظائف العامة
- [ ] التنقل بين الأقسام الفرعية
- [ ] عمل زر الإلغاء بشكل صحيح
- [ ] ظهور رسائل النجاح/الخطأ
- [ ] تحديث الجداول فوراً
- [ ] حفظ البيانات في قاعدة البيانات

## Future Enhancements (تحسينات مستقبلية)

1. **إدارة تصنيفات الموردين**
   - إضافة قسم لتصنيفات الموردين
   - ربطها بجدول supplier_categories

2. **استيراد/تصدير البيانات**
   - استيراد البيانات من ملف Excel
   - تصدير البيانات الحالية

3. **البحث والتصفية**
   - إضافة حقول بحث في الجداول
   - تصفية حسب الحالة (نشط/غير نشط)

4. **Bulk Operations**
   - تفعيل/تعطيل متعدد
   - حذف متعدد

5. **سجل التغييرات**
   - تتبع من قام بإضافة/تعديل السجل
   - عرض تاريخ الإنشاء والتعديل

## Notes (ملاحظات)

- جميع التصنيفات والبيانات المرجعية تُنشأ بحالة "نشط" افتراضياً
- عند حذف مخزن، يتم تحديث القوائم المنسدلة تلقائياً
- يمكن تعديل البيانات بالضغط على أيقونة التعديل في الجدول
- النظام يدعم اللغة العربية بالكامل

## Support (الدعم)

للإبلاغ عن مشاكل أو اقتراح تحسينات، يرجى التواصل مع فريق التطوير.

---

**تاريخ التحديث:** 2026-05-10
**الإصدار:** 1.0.0
**المطور:** Claude Sonnet 4.5
